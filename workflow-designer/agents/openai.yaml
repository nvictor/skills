interface:
  display_name: "Workflow Designer"
  short_description: "Design, checkpoint, and run workflows"
  default_prompt: "Use $workflow-designer to design, inspect, checkpoint, or run a portable finite workflow."
