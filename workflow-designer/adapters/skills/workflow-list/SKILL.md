---
name: workflow-list
description: List portable finite workflow packages beneath the chosen workflow root.
---

# Workflow List

Read and follow the installed `workflow-designer` skill completely, then perform only `workflow-list`.

Treat the remaining invocation text as an optional explicit workflow root. Follow the canonical skill's discovery, read-only, and output contracts without adding domain work or continuity writes.
